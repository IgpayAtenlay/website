<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="css/general.css" rel="stylesheet">
        <title>
            <% if (locals.pageTitle) { %>
                <%= pageTitle %>
            <% } else { %>
                Melissa's Website
            <% }%>
        </title>
        <% if (locals.styles) {
            styles.forEach(element => { %>
                <link href="css/<%= element %>.css" rel="stylesheet">
            <% });
        } %>
    </head>
    <body>
        <header>
            <a class="logo" href="/" title="Melissa Home" aria-label="Melissa Home">
                <img src="assets/images/inverseLogo.svg" focusable="false" aria-hidden="true">
                Melissa
            </a>
            
            <nav class="links">
                <a href="/">Home</a>
                <a href="/aboutMe">About Me</a>
            </nav>
        </header>
        <main>